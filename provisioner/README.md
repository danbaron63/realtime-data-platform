# Provisioner

This image is used to provision resources in the services deployed to k8s.

Scripts can be added to this image, which can then be ran in dedicated kube jobs to perform various tasks, such as:
* creating polaris catalog resources.
