apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: data-generator
  namespace: metrics
  labels:
    release: prometheus
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: data-generator
  endpoints:
    - port: metrics
      path: /metrics
      interval: 5s
