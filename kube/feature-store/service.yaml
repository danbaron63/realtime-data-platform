apiVersion: v1
kind: Service
metadata:
  name: feature-store
  labels:
    app: feature-store
spec:
  selector:
    app: feature-store
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 10000
